import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ContactphPage } from './contactph';

@NgModule({
  declarations: [
    ContactphPage,
  ],
  imports: [
    IonicPageModule.forChild(ContactphPage),
  ],
})
export class ContactphPageModule {}
